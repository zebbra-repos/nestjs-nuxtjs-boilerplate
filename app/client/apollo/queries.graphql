query getProfile {
  profile {
    id
    firstName
    lastName
    email
  }
}

query appSettings {
  settings {
    version
    devise {
      confirmation
      password
      registration
      unlock
    }
  }
}

query csrfToken {
  csrf {
    token
  }
}
